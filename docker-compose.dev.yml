services:
  mongodb:
    image: mongo:8
    container_name: goep_core_db
    restart: unless-stopped
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}
    ports:
      - "${MONGO_PORT}:${MONGO_PORT}"

  minio:
    image: quay.io/minio/minio
    container_name: goep_core_minio
    restart: unless-stopped
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":${MINIO_CONSOLE_PORT}"
    ports:
      - "${MINIO_API_PORT}:${MINIO_API_PORT}"
      - "${MINIO_CONSOLE_PORT}:${MINIO_CONSOLE_PORT}"
    volumes:
      - ./${APP_NAME}_data/minio_data:/data

  # uncomment if you want to try the full app make sure to create the image
  # y en el  .env usa el nombre de los contenedores minio y mongodb en ves de localhost
  #goep-core:
  #  image: goep-core:latest
  #  container_name: goep_core
  #  restart: unless-stopped
  #  env_file:
  #    - .env
  #  ports:
  #    - "8001:8001"
  #  depends_on:
  #    - mongodb
  #    - minio